name: autofix.ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  autofix:
    uses: so1ve/workflows/.github/workflows/rust-autofix-ci.yml@v1
    with:
      toolchain: nightly
